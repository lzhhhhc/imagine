导入工作流时，参考图和提示词会自动检索并绑定。

- 只有一张参考图时，导入即绑定为手机选图
- 提示词隔着参考 Latent、条件节点时也会顺着链路找到；正面和负面是同一句时只绑一次
- 检测、分割用的旁路提示词不会被当成画面提示词
- 一条链上有两句文本，或多个参考图时，不猜测，留给手动绑定
- 步数、CFG 仍不自动绑定

已经保存的工作流不会被改写。要套用新规则，把 API JSON 重新导入一次。
